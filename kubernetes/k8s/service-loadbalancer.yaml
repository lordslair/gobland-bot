apiVersion: v1
kind: Service
metadata:
  name: gobland-it-lb
  labels:
    tier: backend
spec:
  type: LoadBalancer
  selector:
    app: nginx
    tier: backend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
